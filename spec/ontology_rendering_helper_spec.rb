require "spec_helper"

describe OntologyRenderingHelper do
  it "has a version number" do
    expect(OntologyRenderingHelper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
